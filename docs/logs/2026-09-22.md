# 2026-09-22

## Done
- Worked through `docs/findings-develop.md` (yesterday's research-only findings pass) in its own suggested order, fixing everything except one explicitly-deferred DB change:
  - **A1 (security):** deleted the dead credentials subsystem - `apps/api/src/modules/credentials/` (router, internal router, repository) and `lib/crypto.ts` - and their route registrations. It had zero live callers (client side was already removed on 2026-09-21); the internal route was still handing back a plaintext API key over a non-constant-time bearer check. Removed the now-dead `CREDENTIALS_ENCRYPTION_KEY`/`RUNTIME_INTERNAL_TOKEN` env vars from `env.ts` and both `.env.example` files (`apps/api`, `apps/agent-runtime` - the latter's `AURA_API_URL` was dead too, only ever used by the already-removed `credentials-client.ts`). Left the `coding_agent_credentials` table/migration untouched - a destructive DB change needs separate confirmation.
  - **A2 (security):** added `assertSafeOrValue()` (new `apps/api/src/lib/postgrest.ts`) guarding every value hand-interpolated into a PostgREST `.or()` filter string in `runs.repository.ts`/`approvals.repository.ts` - defense-in-depth against a future less-trusted caller silently corrupting the filter.
  - **B1 (perf):** `countByStatus` was firing 8 queries (one per `RunStatus`); now one `select("status")` tallied in JS.
  - **B2 (perf):** added a shared `createCache(ttlMs)` helper in `runtime.client.ts` and applied it to `listAgents` (refactored off its bespoke cache) plus workspace/QA-workspace epics+files, Docker runs, and test runs - previously only `listAgents` was cached.
  - **B3 (perf):** `GET /users` now pages fully through both Supabase Admin's `listUsers` and the `profiles` table instead of silently truncating past 1000 rows with no signal to the caller.
  - **C1 (perf):** `DockerRunsPanel` was polling every 5s and fetching every container, filtering client-side. Added a real `?epic=` filter end to end (`docker ps --filter label=aura.epic=...` in agent-runtime, through the `apps/api` proxy, to the web client) and raised the poll interval to 20s.
  - **C2 (perf):** `WorkspacePage` did a full `GET /threads` refetch after every send/decide turn just to pick up one thread's title/timestamp. `useConversation` now exposes the `thread` row it already gets from each history load; `WorkspacePage` patches just that entry into `threadsState` instead.
  - **C3 (perf):** `JiraPage`'s epics request was gated on the status request finishing first, serializing two round trips. Now fires in parallel (its error is naturally hidden until `configured` is true, since that's when the component showing it mounts).
  - **C4 (perf):** added in-flight GET de-dup to the shared web API client (`shared/api/client.ts`), keyed by path and self-cleaning once the request resolves, so two components mounting the same hook on one page share one round trip instead of doubling it. Scoped this narrower than the finding's full suggestion (a response cache + abort-on-unmount) - the de-dup alone fixes the concrete risk named (duplicate GETs from a double-mounted hook) without needing to thread an `AbortSignal` through every `api.*` call site in the app for a currently-hypothetical benefit.
- All three apps (`apps/api`, `apps/web`, `apps/agent-runtime`) typecheck clean after every change.

**Session 2 (QA/Tester pipeline, model fixes, Jira/CI/manual-edit features):**
- Ran a real, DB-logged QA/Tester session against KAN-36 (Gate 6 filing + a Gate 7 attempt) through the actual `apps/api` chat flow (not a runtime bypass), authenticated as the seeded `qa_engineer` test account. Found and fixed two real model-reliability bugs surfaced by that run instead of only reporting them:
  - Orchestrator model (`groq/qwen/qwen3.8-27b`) reliably (3/3, verified directly against Groq) fails Groq's native tool-calling on the Orchestrator's real 10-tool schema - emits a literal `<tool_call>` text block instead of a structured call. Switched `ORCHESTRATOR_MODEL_ID` and `PO_MODEL_ID` to `groq/openai/gpt-oss-120b` (registry.ts).
  - Gemini fallback (`gemini-3.5-flash`) is capped at 20 requests/day and was already exhausted mid-session. Switched to `gemini-3.5-flash-lite` (500/day, verified with a real tool call). Rejected Gemma 4 (26B/31B) despite a far larger quota - given a tool schema it never emits a structured function call, only prose.
  - Documented (comment only - no Anthropic key exists, nothing implemented) the recommended path if a Claude subscription is added: Sonnet 5 / Haiku 4.5 only, never Opus/Fable, prompt caching on each agent's system instructions as the main token-saving lever.
- Added the developer/tester defect back-and-forth: `delegate_to_test`'s new `file-defect` mode files a real Jira Bug from a failed Gate 7 run, comments the Task, and moves it back for rework; Gate 7 now offers it when `failed > 0`.
- Added manual hand-edit, gated by role: Dev workspace (`PUT /dev-workspace/:epicKey/:discipline/file`, developer only) and QA workspace (`PUT /qa-workspace/:epicKey/file`, qa_engineer only) - same narrow overwrite-only pattern as the Architect's existing design-doc edit, wired into the Scaffolded Project Files and QA Files & Test Runs pages.
- Redesigned the Jira page: removed the embedded Architect-workspace "Documents" section (duplicated the standalone Design Documents page); added a real comment thread (`GET`/`POST /jira/issues/:key/comments`, new in `apps/api`'s Jira REST client) and a "Bugs" section (`getEpicChildren` now buckets Bug too).
- Added colocated tests + local CI:
  - `delegate_to_qa`'s `file` step now also copies each scenario into the matching scaffolded project's own test directory (`ui` → `<frontend>/tests/`, `api` → `<backend>/test/e2e/`), best-effort per discipline, alongside the unchanged canonical copy Gate 7 reads from.
  - Gate 4's `execute` now also writes `.github/workflows/<discipline>-ci.yaml`, a `.gitignore` if missing, and runs `git init` (idempotent, no remote, no push).
  - New tool `delegate_to_ci` - project-wide (epicKey + discipline, no taskKey - a scaffolded project isn't owned by one Task, corrected mid-build after being caught): `run` executes the same checked-in CI steps locally in Docker, ungated; `file-defect` files a Jira Bug under the Epic from a failed run, gated/self-approved. No GitHub integration anywhere - no repo creation, no push, no PAT, no Actions polling.
  - Backfilled KAN-36's already-scaffolded backend/frontend (predates this addition) with the same CI/gitignore/git-init/colocated-specs, generated from the real `CI_STEPS` source so it matches a fresh Gate 4 scaffold exactly.
- Added inline UX: `RunCiModal` (drives `delegate_to_ci` via a throwaway Orchestrator thread, rendered in place with the same `ToolActivity`/`GateCard` primitives the Workspace chat uses) so running CI never navigates away, and `TaskModal` (Jira detail + transitions + comments) so a Task can be viewed without leaving either page. Both pages now list the Epic's real Jira Tasks directly instead of a manual key-entry box.
- Role-tailored QA Files & Test Runs for the one shared route `qa_engineer`/`tester` both use: QA Engineer keeps the hand-edit controls; both get Run tests/Run CI, with a hint clarifying whose approval a Tester's request needs.

## Decisions
- Left `coding_agent_credentials`'s table/migration in place rather than dropping it - per the findings doc's own recommendation, a real destructive DB change should be confirmed separately, not bundled into a pure-deletion cleanup pass.
- Declined to build C4's full request-cache + unmount-cancellation layer now - the doc itself frames it as "not urgent today," and building it out would mean either an app-wide `AbortSignal`-threading refactor or dead, unused scaffolding. Did the contained, immediately-useful half (in-flight de-dup) instead.
- Fixed (not just documented) the two model-reliability bugs found while testing, per explicit instruction to take LLM failures into my own hands during the evidence-gathering session rather than only reporting them.
- Kept Gate 7 (`delegate_to_test`) Task-scoped (it closes out one specific Jira ticket) while making `delegate_to_ci` Epic+discipline-scoped instead (a scaffolded project has no single owning Task) - caught and corrected mid-build after being pointed out.
- Declined any GitHub API integration (repo creation, push, PAT-based Actions-status polling) - local Docker CI gives the same before-you-push signal with zero new credentials.
- Kept "QA Files & Test Runs" one shared route for `qa_engineer`/`tester` (role-tailored rendering) rather than a separate Tester page.

## Blockers / open questions
- Carried over from 2026-09-21: `0004_coding_agent_credentials.sql` still hasn't been applied, and is now more clearly a candidate to drop instead, once confirmed.
- Gate 6/7 for KAN-36 from the DB-logged session was intentionally left mid-flight (Gemini's daily quota was hit) rather than burning more quota to force a retry this session.

## Next
- Confirm whether to drop the `coding_agent_credentials` table/migration now that its code is gone.
- Actually exercise the new pipeline end to end against KAN-36: run `delegate_to_ci` for real (Docker), retest Gate 7, and try `file-defect` on a real failure - everything from session 2 was typechecked, not run live.
- Manual browser verification of the Jira page, Docker runs panel, Workspace thread list, and now also RunCiModal/TaskModal/role-tailored QA page - none of this has been opened in a browser yet.
- Decide whether to commit the current uncommitted changes.
