// The warm AURA agent palette, shared by the running-agent icon (agentLive.tsx) and the per-agent
// crystals (agentIcons.tsx). In its own module so the icon files export only components (React
// Fast Refresh).
export const PALETTE = ["#FFB800", "#FF6A00", "#E91E63", "#7B1FA2", "#C2185B", "#E30613"];
